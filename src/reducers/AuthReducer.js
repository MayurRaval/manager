import { EMAIL_CHANGED } from '../actions/types';

const INITIAL_STATTE = { email: ''};

export default (state =INITIAL_STATTE ,action) => {
  switch(action.type){
      default:
        return state;
  }
};
